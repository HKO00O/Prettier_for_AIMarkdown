<%*
// 1. 获取系统剪贴板内容
const clipboard = await tp.system.clipboard();

// 2. 🔴 注意这里：因为导出来的是对象，需要解构出里面的 formatAiMarkdown 函数
const { formatAiMarkdown } = tp.user.prettier;

// 3. 调用函数清洗
const cleanedText = formatAiMarkdown(clipboard);

// 4. 输出
tR += cleanedText;
-%>